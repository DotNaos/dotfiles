# Persistent dotfiles context overrides.
#
# Copy the values you want into:
#   ~/.config/dotfiles/context.env
#
# Direct environment variables still win for one-off runs.

# DOTFILES_PLATFORM=macos
# DOTFILES_ROLE=default
# DOTFILES_MODE=interactive
# DOTFILES_SHELL=zsh
